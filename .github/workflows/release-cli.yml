name: publish

on:
  push:
    tags:
      - 'cli/v*'

jobs:
  build:
    name: Build binaries
    #strategy:
    #  matrix:
    #    os: [ubuntu-latest, arm64-ubuntu-22, macos-latest, macos-13, windows-latest]
    #    include:
    #      - os: ubuntu-latest
    #        GOOS: linux
    #        GOARCH: amd64
    #      - os: arm64-ubuntu-22
    #        GOOS: linux
    #        GOARCH: arm64
    #      - os: macos-latest
    #        GOOS: darwin
    #        GOARCH: arm64
    #      - os: macos-13
    #        GOOS: darwin
    #        GOARCH: amd64
    #      - os: windows-latest
    #        GOOS: windows
    #        GOARCH: amd64
    #runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [windows-latest]
        include:
          - os: windows-latest
            GOOS: windows
            GOARCH: amd64
    runs-on: ${{ matrix.os }}
    steps:
      #- uses: actions/checkout@v4
      #- uses: actions/setup-node@v4
      #  with:
      #    node-version: 20
      #- uses: pnpm/action-setup@v3
      #  with:
      #    version: 9
      #- uses: actions/setup-go@v5
      #  with:
      #    go-version: '1.22.7'
      #    cache: false  
      #- name: Run build script
      #  working-directory: ./
      #  run: make build
      #- name: Append os/arch to binary file name
      #  run: mv bin/kubetail bin/kubetail-${{ matrix.GOOS }}-${{ matrix.GOARCH }}
      - name: Calculate sha256 checksum
        shell: bash
        run: |
          FILE=bin/kubetail-${{ matrix.GOOS }}-${{ matrix.GOARCH }}
          OUTPUT_FILE=${FILE}.sha256
          echo "blahblah" > "$FILE"
          if [ "$RUNNER_OS" == "Linux" ]; then
            sha256sum "$FILE" | cut -d " " -f 1 > "$OUTPUT_FILE"
          elif [ "$RUNNER_OS" == "Windows" ]; then
            certutil -hashfile "$FILE" SHA256 > "$OUTPUT_FILE"
            cat "$OUTPUT_FILE" | findstr /V "SHA256" | findstr /V "CertUtil" > "$OUTPUT_FILE"
          elif [ "$RUNNER_OS" == "macOS" ]; then
            shasum -a 256 "$FILE" | cut -d " " -f 1 > "$OUTPUT_FILE"
          fi
      - name: Attach files to gh release
        uses: softprops/action-gh-release@v2
        with:
          files: bin/*
          draft: true
